meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: null
  command: python bot.py
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
