meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"

build:
  command: pip install -r requirements.txt

run:
  command: python bot.py
  persistenceMount: /data
  containerPort: 80
